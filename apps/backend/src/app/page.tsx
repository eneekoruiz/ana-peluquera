export default function BackendHealthPage() {
  return (
    <main style={{ padding: 24, fontFamily: "system-ui, sans-serif" }}>
      <h1>AG Beauty Salon backend is running</h1>
      <p>Use the API routes under <code>/api</code>.</p>
    </main>
  );
}

